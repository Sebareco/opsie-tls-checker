{
  "name": "opsie-tls-checker",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,ui -c blue,green \"bun run --cwd backend dev\" \"bun run --cwd frontend dev\"",
    "build": "bun run --cwd frontend build",
    "start": "bun run build && bun run --cwd backend start"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "concurrently": "^9.2.1"
  }
}
